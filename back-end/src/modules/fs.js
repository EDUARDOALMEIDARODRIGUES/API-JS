/*  import { error } from 'node:console';
import fs from 'node:fs';
import path from 'node:path';

fs.mkdir(path.join(import.meta.dirname, '/test' ), (error) => {
      if (error) {
            return   console.log(" Erro: ", error);
      };

      console.log("feito");
})

fs.watchFile(path.join(import.meta.dirname, '/test','test.html', ), 'ola node??', (error) => {
      if (error) {
            return   console.log(" Erro: ", error);
      };

      console.log("feito o arquivo");
})*/